import { types } from "../../types/counter/types"

//actions
export const sumar = {
    type: types.SUMAR
}
export const restar = {
    type: types.RESTAR
}
export const reset = {
    type: types.RESET
}

